# Final locator index: verified raw-HTML map is at candidates/locator_map.json.

Section grounding re-verified in candidates/locator_verified_table.json (deterministic strip procedure, frozen HTML bytes). Stale-path readings are corrected in PATH_ALIASES_CORRIGENDUM.md.
