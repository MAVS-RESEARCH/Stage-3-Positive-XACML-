# NON_BLIND_HARDENING_REVIEW -- Wave-2 H/Lambda depth sweep (developmental only)

## NEW OBJECTIONS
W2-01 Env-validation path survives zero-designator proof. OPTIONAL helper beginIndividualDecisionRequest validates current-dateTime/date/time consistency whenever dateTimeBag!=null, throwing syntax-error Indeterminate even if policy reads no env. Census covers frozen request only implicitly; no mechanical census over repaired bytes that env Attribute count stays 0; no pin that insert_attribute cannot touch environment. Cat A. Material: breaks generality of resolved==request; low for frozen triple if closed. Close: N4 validation-gate excerpt + zero-env-Attribute census over all three bytes + builder category-pin line.
W2-02 Wall-clock latent state excluded. overrideEvalCtxFromTimestamp reads getCreationTimestamp (system Instant); same bytes at different times have different latent bags. Timestamp source/timezone not in resolved_effective or manifest hash. Cat C. Material: constancy across repairs undefined. Close: pin timestamp source/UTC; exclude clock from hash with inertness reason tied to W2-01 census.
W2-03 (child.text or "") verbatim fiction. derive_H canonicalization (no trim, single-text-node, None/""/tails/comments/PIs, Attribute-vs-Value DataType conflicts) vs N4 AttributeValueFactoryRegistry lexical parsing unevidenced. Cat D. Material to value-identity. Close: factory excerpts + admitted-value census or align derive_H.
W2-04 Content/repeated-Category dropped without census. No census that all three lack Content/distinct Categories; duplicate-Category throws MDP Indeterminate while H would merge. Cat B. Material to H-as-full-context. Close: mechanical census + requireContentForXPath wiring.
W2-05 Issuer "" vs absent + flag outside hash. JAXB null-vs-"" normalization uncited; no empty-string census. strictAttributeIssuerMatch=false in resolved_effective but manifest canonical is identifier-only: flipping flag changes matching without changing pre_hash. Cat C. Material: silent capability change. Close: census + excerpt + include flag in hashed components.
W2-06 Manifest recorded-but-unhashed: resolved_effective flags, dir listing, ignoreOldVersions, custom providers, std override unhashed; derive_Lambda still narrow. Second policy file / added provider / flag flip leaves pre_hash unchanged. Cat D, BLOCKING for constancy. Close: hash full pdp.xml C14N or all defaults + listing + provider elements; pin comparator; recompute.
W2-07 PARENT_DIR path-dependence. Manifest hashes placeholders not resolved URI (absolute, file://, slash-strip, Windows separators); glob ordering/multi-root unevidenced; listing is point-in-time. Cat C. Material. Close: hash resolved URI + sorted listing + glob/ordering excerpts. (Note: absolute URI harms cross-machine reproducibility; relative-layout rule + listing preferred.)
W2-08 Deferral promissory + version under-pinning. No sealed comparator bytes/abort demo; version pins source triple only (no jar/xsd/extension/JVM/lxml); H duplicate-merge rule unpinned. Cat D/C. Material: constancy untestable pre-repair. Close: seal comparator + abort demo on mutated input, or mark PENDING; extend version record or scope Lambda to source-level.

## ACCEPTED WAVE-1 CLOSURES (freeze-time point facts)
H-02/A-01/A-04 bytes+diffs; H-03 issuer rule + zero census; H-04/H-08 env recorded; H-06 route-a scoped; H-07 absent markers; P_R-03/04/05/06; L-06 std-env corrected; L-07 default-lax; G-03 map; G-04/05/06/08 procedures; G-02 hygiene.
