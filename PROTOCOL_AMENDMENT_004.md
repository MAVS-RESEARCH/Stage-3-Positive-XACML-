# PROTOCOL AMENDMENT 004 — Post-Certification Semantic Development (Phase 2D)

Experiment: `PC-XACML-S3PLUS-v1` (unchanged; no fork, no new ID, no reset).
Type: prospective post-failure developmental amendment, committed as a new
commit. No previous commit, panel, freeze, seal, or result is amended,
squashed, deleted, overwritten, or concealed. `artifacts/seal/FINAL_RESULT.json`
(`NATIVE_ANCHOR_INSUFFICIENT` over Revision 002) remains immutable history.

## 1. Operative state at amendment time (verified from working checkout)

- repository commit: `92d9616`; external specimen unchanged (AuthzForce
  `3cc0e988`, XACML frozen, fixture/policy/pdp.xml untouched)
- target audit NEVER ran; completed-world PDP evaluations = 0
- touch/K/freezes NOT_COMPUTED; Phases 3+ NOT_RUN
- historical panel complete and preserved: AUD-C01/C02/C03 valid records
  (H 3xFIXED, P_R 3xFIXED, Lambda 3xPARTIAL, Atom 2xFIXED/1xPARTIAL),
  gate exit 3, negative seal written
- zero downstream Stage-III behavior observed (nothing to leak)

## 2. Scientific boundary

MAY learn from C01/C02/C03 semantic criticisms: definitions, evidence,
proofs, provenance, pre-existing structure exposure, implementation/audit
defect correction, packet completeness. MAY NOT run/examine completed target
worlds/decisions, derive touch, compute K/freezes, change expected
touch/K/classification, or alter targets/worlds/costs/AuthzForce semantics.
Every mutation improves fidelity to the already-existing external system:
change epistemic access, never reality to fit PC.

## 3. No FIXED-optimization

Objective: eliminate every source-grounded material ambiguity from the failed
panel + adversarial analysis — NOT "mutate until certifiers say FIXED".
Allowed only with exact objection + frozen-source analysis + source-grounded
repair + unchanged specimen. Development stops at
`NO_UNRESOLVED_SOURCE_GROUNDED_MATERIAL_DEFECT`; later certification is an
outcome of that criterion, never its definition. Code must never loop
`while not positive`.

## 4. Preserve failure as POST_CERT_HARDENING_ROUND_000

`artifacts/audits/post_cert_hardening/rounds/POST_CERT_HARDENING_ROUND_000/`
holds immutable references (hashes + paths, copies never edited) to all
C01/C02/C03 raw/verdict/provenance records, the failed gate result, prior
freeze hash, historical FINAL_RESULT hash, and zero-execution proof. The
certifiers remain genuine final certifiers of Revision 002; the failure
remains real. Their criticisms seed the new revision's attack set.

## 5. Subagents (NON-BLIND developmental workers)

Authorized aggressively in parallel; may inspect failed reports, all history,
definitions, corpus, implementation, hashes, certificates, rehearsals, prior
objections. NEVER count as certifiers. Minimum battery: Lambda closure,
Lambda hash/reconstruction, Lambda lifecycle/equality, Atom boundary, Atom
staging/checkpoint, provenance/completeness, alternative-semantics red team,
cross-anchor red team, bypass tester. No user checkpoints between legitimate
repair iterations.

## 6. Development targets

Lambda (3xPARTIAL): first-principles reconstruction; smallest complete
externally justified extensional boundary — every evaluation-capable
component either pinned (ID, basis, locator, inclusion reason, canonical
form, bytes, SHA-256, reconstruction procedure, stability rule,
request/env/default flags) or proven elsewhere/constant. Pre_hash
reconstructible from packet-visible artifacts only (ordered manifest,
canonicalization, constituent hashes, composition algorithm, recompute tool,
independent implementation, test vector; expose-or-explain every gap).
Post_hash solved WITHOUT target evaluation: `Lambda_before_request_repair`
vs `Lambda_after_request_repair_pre_evaluation` over the same manifest
(identical set/bytes/hashes/composite) — capability/configuration equality,
not decision equality; if insufficient, mark irreducible, never force, never
execute PDP for it. Engine closure: pin/expose release/commit, built JAR
hashes, dependency manifest, JDK identity, parser/preprocessor/provider
implementations, entrypoint — per-artifact capability analysis, neither
under- nor over-inclusion.
Atom (1xPARTIAL): 9-point boundary evidence (native/ authored/staging/
invocation boundaries, no-logic proof, response-availability order,
no-undeclared-artifact proof, world-byte correspondence, post-boundary PDP
start) via a mechanically inspectable staging certificate (hashes, diffs,
paths/realpaths, dir-set + deployment-set manifests, disjointness, wrapper
hash, invocation args, entrypoint, no-prior-response + no-alternate-policy
proofs, before/after manifests). No prose-trust.

## 7. Adversarial discipline + autonomous loop

After every mutation, NEW attackers with different objectives (unpinned
component, hidden default, misassigned request-varying quantity,
H-vs-Lambda misallocation, staging bypass, alternate boundary, independent
hash recomputation, irreproducible artifact, countermodel). Valid material
criticism reopens development. Loop: objections → analysis → attacks →
normalize → verify → mutation → test → invariant → exec==0 → dev packet →
fresh attack → repeat. Stop at resolved-all or proven-irreducible.
Invariant after every mutation (specimen/worlds/targets/Q/cost/order
unchanged). New revision never overwrites 001/002/panels/seal/history.

## 8. Revision 003, burn-in, new panel, failure path, claims

Converged development freezes `SEMANTIC_FREEZE_REVISION_003` (snapshots
preserve 001/002). Burn-in: two clean post-mutation sweeps (fresh agents, no
mutation between, full panels, no VALID_MATERIAL/UNRESOLVED, invariant/tests
PASS, exec/touch/K 0) + recommended taxonomy-free holdout; holdout defect
reopens. Then STOP: new panel MUST be fresh chairs AUD-C04/C05/C06 (never
reuse C01-C03), receiving only new packet + frozen prompt + schema;
unanimous FIXED on H/P_R/Lambda/Atom required. Later failure: preserve,
reopen only on concrete grounded defect with wholly new panel (never seat a
fourth chair over one revision); irreducible ambiguity without specimen
change → record underidentification, never lower the standard. Manuscript
discloses the full sequence (hardening → failed panel + sealed negative →
post-cert development → new revision → fresh panel → zero pre-cert target
execution); never one-shot-success/human-validation language.
